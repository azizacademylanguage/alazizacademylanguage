{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "if [ -f manage.py ]; then python manage.py collectstatic --noinput; else cd backend && python manage.py collectstatic --noinput; fi"
  },
  "deploy": {
    "startCommand": "if [ -f manage.py ]; then bash start.sh; else cd backend && bash start.sh; fi",
    "healthcheckPath": "/health/",
    "healthcheckTimeout": 300,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
